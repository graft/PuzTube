<div id="roundtitle" class="rounded_title">
    <div style="padding: 2px;">
    <b><%= link_to_remote 'Show', :url => info_round_path(:id=>round.id), :update => 'ROUND'+round.id.to_s %></b> |
    <b><%= link_to_function 'Update', "javascript: $('r#{round.id}.update').onsubmit();" %></b>
    </div>
  </div>
  <div style="padding: 5px;">
  <% form_remote_for(round,
                  :url => { :action => 'update', :controller => 'rounds', :id => round.id },
                  :method => :put,
                  :html => { :id => "r#{round.id}.update" } ) do |f| %>
    <b><%= f.label :name %>:</b>
    <%= f.text_field :name %><br>
    <b><%= f.label :url %></b>
    <%= f.text_field :url %><br>
    <b><%= f.label :hint %></b>
    <%= f.text_field :hint %><br>
    <b><%= f.label :priority %></b>
    <b><%= select :round, :priority, "<option value=''>Select priority</option>" + options_for_select(Round.priority_options,round.priority) %><br>
    <b><%= f.label :captain %></b>
    <%= collection_select(:round,:captain,User.all,:login,:login,{:prompt=>true}) %><br>
    <b><%= f.label :answer %></b>
    <%= f.text_field :answer %><br>
  <% end %>
</div>

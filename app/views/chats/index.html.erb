<h1>Listing chats</h1>

<table>
  <tr>
    <th>User</th>
    <th>Puzzle</th>
    <th>Text</th>
  </tr>

<% @chats.each do |chat| %>
  <tr>
    <td><%=h chat.user %></td>
    <td><%=h chat.chat_id %></td>
    <td><%=h chat.text %></td>
    <td><%= link_to 'Show', chat %></td>
    <td><%= link_to 'Edit', edit_chat_path(chat) %></td>
    <td><%= link_to 'Destroy', chat, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New chat', new_chat_path %>